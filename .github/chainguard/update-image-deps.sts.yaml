issuer: https://token.actions.githubusercontent.com
subject: repo:some-natalie/kubernoodles:ref:refs/heads/automation/update-image-deps-.*
claim_pattern:
  job_workflow_ref: some-natalie/kubernoodles/.github/workflows/update-image-deps.yml@.*

permissions:
  contents: write
  pull-requests: write
